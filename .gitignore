# Secret — 絶対にCommitしない
.env*
!.env.example
*.pem
*.key
*.p12
*.pfx
*-service-account*.json
credentials.json

# Node / Frontend
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Python / Backend
backend/.dev-assets/
backend/.dev-job-input/
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Cloud / Deploy
.firebase/
firebase-debug.log

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# 生成物（Assetは scripts/generate_mock_assets.py で再生成できる場合はCommit不要だが、
# 共通Fixtureとして全担当が同じものを使うため contracts/assets/ は追跡する）
/tmp/

# ローカルPoC・モデルキャッシュ
# 入力データ、評価出力、デバッグ画像、調査用の鍵を含み得るため追跡しない。
# READMEで利用する公開用の発表画像は assets/ に置き、こちらには含めない。
/poc-images/
/poc-output/
/backend/.models/

# ルート直下で行う一時調査メモ
prompt.md

# Test / coverage artifacts
coverage/
.coverage
htmlcov/
*.tsbuildinfo
